suite: redis-deployment
release:
  name: hmac-manager
values:
  - values/base.yaml
templates:
  - redis-deployment.yaml

tests:
  - it: renders the bundled Redis Deployment when redis is enabled
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: Deployment
      - equal:
          path: metadata.name
          value: hmac-manager-redis
      - equal:
          path: spec.template.spec.containers[0].name
          value: redis

  - it: deploys to the configured namespace
    set:
      namespace: my-namespace
    asserts:
      - equal:
          path: metadata.namespace
          value: my-namespace

  - it: renders no Deployment when redis is disabled
    set:
      redis.enabled: false
    asserts:
      - hasDocuments:
          count: 0
