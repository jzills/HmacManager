apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hmac-manager.fullname" . }}-config
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "hmac-manager.labels" . | nindent 4 }}
data:
  config.json: {{ include "hmac-manager.policyConfig" . | quote }}
